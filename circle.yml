general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    - exit 0

deployment:
  production:
    branch: master
    commands:
      - chmod +x deploy.sh
      - ./scripts/deploy.sh app